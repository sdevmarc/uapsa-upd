{
  "buildCommand": "npm run build; npm run start:prod",
  "installCommand": "npm install -g nestjs/cli; npm install",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/main.ts"
    }
  ]
}